REQUIREMENTS

You need the following packages (Debian names):
python-twisted-words
python-feedparser

And Python, of course


INSTALLATION

Create a file named 'config' with a content that looks like the following:

nick:nflbot
pass:nickserv_password
channels:#channel1,#channel2
server:irc.example.org
